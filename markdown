# Stock News Alert

A Python script to fetch stock data and relevant news articles, then send SMS alerts using Twilio.

## Installation

1. Clone this repository.
2. Install dependencies: `pip install -r requirements.txt`
3. Configure your API keys and Twilio credentials in `config.py`.

## Usage

Run the `main.py` script to fetch stock data, news articles, and send alerts.

